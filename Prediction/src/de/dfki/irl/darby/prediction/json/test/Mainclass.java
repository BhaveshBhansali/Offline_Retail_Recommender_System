package de.dfki.irl.darby.prediction.json.test;

import java.util.ArrayList;

import com.stackframe.pathfinder.AStar;

import de.dfki.irl.darby.prediction.grid.Grid;
import de.dfki.irl.darby.prediction.grid.GridCell;
import de.dfki.irl.darby.prediction.grid.GridScorer;

public class Mainclass {

	public static void main(String[] args) {
		/*long now=System.currentTimeMillis();
		Grid grid=new Grid(1000, 1000);
		System.out.println("finished grid in " + (System.currentTimeMillis()-now)/1000 + "s");
		now=System.currentTimeMillis();
		ArrayList<GridCell> targets=new ArrayList<GridCell>();
		targets.add(grid.getCells()[999][999]);
		
		GridScorer scorer=new GridScorer(grid);
		for (int i=0;i<10000;i++){
			scorer.evaluatePosition(grid.getCells()[30][50], 0.8);

		}
		System.out.println("evaluated in " + (System.currentTimeMillis()-now) + "ms");*/
		
		
	}

}
